# Frontend for Secure Code Execution Platform

This frontend app is built with React, Vite, and Tailwind CSS.

## Install

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

The app proxies `/api` to `http://localhost:8000`.
